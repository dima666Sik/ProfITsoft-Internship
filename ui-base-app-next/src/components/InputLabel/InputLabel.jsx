import React from 'react';
import InputLabelMui from "@mui/material/InputLabel";

const InputLabel = ({message}) => {
    return (
        <InputLabelMui>{message}</InputLabelMui>
    );
};

export default InputLabel;
